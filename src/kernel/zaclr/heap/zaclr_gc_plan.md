# ZACLR GC scaffold note

The heap subsystem is intentionally limited to ownership contracts during M1.

Allocator behavior, object layout, tracing GC, and exception interaction are deferred to later milestones.
